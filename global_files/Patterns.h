#pragma once

#include <type_traits>

char* generateLPS(char* pattern, size_t patternSize)
{
	
}

//template<
//	typename T, 
//	typename = std::enable_if<std::is_pointer> 
//>
//T* patternScanRange(char* pattern, size_t patternSize, void* beginRange, size_t sizeOfRange) 
//{
//	// KMP algorithm
//}